import { PagePlaceholder } from "@/components/dashboard/page-placeholder";

export default function CustomersPage() {
  return (
    <PagePlaceholder
      title="Kupci"
      description="Cross-platform lista kupaca sa dedup-om po email-u — Faza 2."
    />
  );
}
